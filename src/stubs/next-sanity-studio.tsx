import type { Metadata, Viewport } from "next";

export const metadata: Metadata = {
  title: "The Base Studio",
};

export const viewport: Viewport = {
  width: "device-width",
  initialScale: 1,
};

export function NextStudio() {
  return null;
}
